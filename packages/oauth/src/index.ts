/**
 * @entry/oauth — DEPRECATED.
 *
 * Better Auth (packages/auth) now handles all social sign-on (Google, GitHub)
 * via its built-in socialProviders config. This package is kept as an empty
 * stub for backward compatibility — nothing imports it anymore.
 */
export {};
